// Создать 3 переменные разных
// типов и вывести в консоль 
//для каждой из них строку следующего вида:    

//     `Variable: %variable name% have type: %type variable%`


const num = 100;
console.log(`num have type: ${typeof num} `)

const str = 'string';
console.log(`str have type: ${typeof str} `)

const bool = true;
console.log(`bool have type: ${typeof bool} `)